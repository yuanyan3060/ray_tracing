[package]
name = "ray_tracing"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.32.1"
image = "0.25.9"
rand = "0.10.0"
rayon = {version = "1.11.0", optional = true}
tobj = "4.0.3"

[features]
default = ["rayon"]
parallel = []
rayon = ["dep:rayon", "parallel"]
